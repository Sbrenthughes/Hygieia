/**
 * Controller for the modal popup when creating
 * a new dashboard on the startup page
 */
(function () {
    'use strict';

    angular
        .module(HygieiaConfig.module)
        .controller('CreatePropertyController', CreatePropertyController);

    CreatePropertyController.$inject = ['propertyManager'];
    function CreatePropertyController(propertyManager) {

        var ctrl = this;

        // public variables


        // public methods

    }
})();
